# [Project Name]

## Project Overview
<!-- What does this project do? Who is it for? What problem does it solve? -->
A [brief description] for [target users]. [One sentence on the core value prop].

## Tech Stack
- **Language**: TypeScript / Python / Go / ...
- **Framework**: Next.js / FastAPI / ...
- **Database**: PostgreSQL / MongoDB / ...
- **Key Libraries**: [list important ones]
- **Package Manager**: npm / pnpm / bun / ...

## Architecture
<!-- How is the code organized? Key patterns used? -->
- `src/` — Main source code
- `tests/` — Test suite
- `docs/` — Documentation

Key patterns: [REST API / event-driven / monorepo / microservices / ...]

## Common Commands
```bash
npm run dev       # Start dev server
npm test          # Run tests
npm run build     # Production build
npm run lint      # Lint code
```

## Code Conventions
- [Naming conventions, e.g. camelCase for vars, PascalCase for components]
- [Testing requirement, e.g. unit tests required for all new functions]
- [Import style, formatting tool, etc.]
- [Error handling patterns]

## Key Constraints
<!-- Things Claude MUST know to avoid mistakes -->
- Do NOT modify `[critical file]` without also updating `[related file]`
- Always run `npm test` before suggesting a commit
- Environment secrets live in `.env.local` — never hardcode them
- [Any other critical rules]

## Current Focus
<!-- Optional: what are you actively working on? Update as needed -->
- [ ] [Current task or milestone]
